#pragma once
// Test stub for the build-generated IconData.h (normally produced from assets/img/Icon.png
// by the root CMake). Overlay.cpp includes this; the icon bytes are only used in
// Render(), which the unit tests never call.
inline constexpr unsigned char kIconData[] = {0};
inline constexpr unsigned int kIconDataSize = 0;
