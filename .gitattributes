# Normalize all text files to LF in the repo and on checkout (incl. Windows).
* text=auto eol=lf

# Tracked binaries: auto-detection is reliable, but be explicit.
*.png binary
*.ico binary
*.ttf binary
